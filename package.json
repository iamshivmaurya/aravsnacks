{
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "api": "cd api && python3 -m uvicorn main:app --reload --port 8001",
    "dev": "npm-run-all --parallel frontend backend api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
